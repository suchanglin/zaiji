Vue.js是用于构建交互式的Web界面库。Vue.js集中在MVVM模式上的视图模型层，并通过双向数据绑定连接视图和模型。

Vue.js不支持IE8及其以下的版本。

在实例化Vue时，需要传入一个  选项对象，它可以包含 数据 模板 挂载元素 方法 生命周期钩子     

var MyComponent = Vue.extend({

})
var xxoo = new MyCoponent();


created comiled ready destroyed 


Vue模板从根本上不同于基于字符串的模板。

binding 捆绑，绑定

observe 观察，检测，监测

init 开始，初始

compiled 汇编

destroy 销毁，破坏

capitalize 大写

raw 原生的，未加工的

literal原意的，字面的

v-bind v-on

https://cdn.bootcss.com/vue/2.2.2/vue.min.js

https://unpkg.com/vue/dist/vue.js

v-if template v-if  v-show(display,不支持template) v-else v-model
v-for   $index $key

push() pop() shift() unshift() splice() sort() reverse()
event.preventDefault()  event.stopProgation()

$on() $emit()  $dispatch()  $broadcast()

keep-alive   active   transition-mode

enter tab delete esc space up down left right 

el vm expression arg name modifiers descriptor 

插件：vue-router vue-resource vue-async-data vue-validator
vue-devtools  vue- touch  vue-element 

vue-简单、易学，指令v-xxx，一片html代码配合上json,在new出来vue实例。个人维护项目，移动项目，小巧

angular--上手难，指令ng-xxx，所有属性和方法都挂在到$scope，PC端


共同点：不兼容低版本IE