var obj = {
    test: {
        nice: {
            fun: function() {
                console.log(this)
            }
        }
    }
}

obj.test.nice.fun();  // this指向nice，而不是调用者obj

//如果一个函数中有this，并且包含该函数的对象也同时被另一个对象所包含，
//尽管这个函数是被最外层的对象所调用，this指向的也只是它上一级的对象。
 
